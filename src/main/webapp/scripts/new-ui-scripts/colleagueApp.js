angular.module('colleagueApp', [])
        .controller('colleagueCtrl', colleagueCtrl)
        .directive('header', header)
        .directive('sidebar',sidebar)
        .directive('footer', footer);
